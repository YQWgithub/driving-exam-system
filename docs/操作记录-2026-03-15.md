# 操作记录（2026-03-15）

## 目标
- 启动并联通项目（后端 + 前端 + 数据库）。
- 修复章节练习、错题本、模拟考试相关的关键阻塞问题。

## 今日已完成
- 完成环境核对：`Java`、`Maven`、`Node`、`MySQL`。
- 新增 Maven 本地配置文件，解决本地仓库路径不可写问题：
  - `D:\Code\测试\maven-settings.xml`
- 修复数据库连接串转义错误（`&amp;` -> `&`）：
  - `driving-exam-service/src/main/resources/database.properties`
- 重新安装并构建后端模块（`-DskipTests install`），后端成功启动至 `8083`。
- 修复前端工程入口链路，切回标准 Vite 结构：
  - `frontend/index.html`
  - `frontend/src/main.js`
  - `frontend/src/router/index.js`
  - `frontend/vite.config.js`（代理改为 `http://localhost:8083/driving-exam`）
- 修复模拟考试开考失败（`paper_id` 为空）：
  - `driving-exam-web/src/main/java/com/drivingexam/controller/ExamController.java`
- 增强交卷判定容错（当试卷为空时默认及格线）：
  - `driving-exam-service/src/main/java/com/drivingexam/service/impl/ExamRecordServiceImpl.java`
- 重写登录页，修复“账号密码输入无法正常删除/清空”：
  - `frontend/src/views/Login.vue`
- 补充题库数据（多条示例题），并导入数据库：
  - `database/seed_questions.sql`

## 联调验证结果
- 前端服务可访问：`http://localhost:5173/`
- 后端服务可访问：`http://localhost:8083/driving-exam`
- 章节练习接口返回题目正常。
- 模拟考试 `start` / `submit` 正常。
- 错题本写入与查询正常。
- 登录接口实测成功账号：
  - `admin / admin123`
  - `student1 / 123456`

## 备注
- 项目中部分旧文件存在历史编码乱码（不影响当前主流程运行）。
- 后续如有功能变更，请同步更新本文件和 `项目内容说明.md`。
