# 操作记录（2026-05-09）

## 目标
- 检查项目结构、构建状态和前后端运行期问题。
- 修复低风险但会直接影响功能可用性的缺陷。

## 今日已完成
- 完成源码结构核对：后端为 Spring MVC + MyBatis 多模块，前端为 Vue 3 + Vite + Element Plus。
- 修复前端统一请求层：
  - 后端 `Result.code != 200` 时转为 rejected Promise，页面可进入错误分支。
  - 保持成功响应统一返回 `Result.data`，避免页面重复读取 `res.data`。
- 修复多个页面误用 `res.data` 导致列表为空的问题：
  - 章节练习、考试历史、后台控制台、题库管理、用户管理、系统配置。
- 接入后台管理路由：
  - `/admin`
  - `/admin/questions`
  - `/admin/users`
  - `/admin/config`
  - 管理路由增加前端角色校验，仅管理员可进入。
- 修复后台题库管理页面模板错误，使其纳入生产构建后可以正常编译。
- 优化前端打包：
  - 路由组件改为懒加载。
  - Vite 按 `vue`、`element-plus`、`axios` 拆分 vendor chunk。
- 修复后端 Spring MVC 扫描范围：
  - MVC 子容器只扫描 `com.drivingexam.controller`，避免重复创建 service bean 并影响事务代理。
- 修复 MyBatis 映射：
  - `exam_record.total` 可正常写入和查询。
  - `wrong_question.wrong_answer` / `correct_answer` 可正常写入、更新和查询。
  - 题目、分类、系统配置、试卷新增时使用数据库当前时间，避免 `create_time` / `update_time` 为空。
- 修复登录接口返回敏感字段：
  - 登录成功返回用户信息前清空 `password`。
- 新增考试记录汇总接口：
  - `GET /exam/all`，用于后台控制台统计考试次数。

## 验证结果
- `mvn.cmd -q -DskipTests package`：通过。
- `npm.cmd run build`：通过。
- 前端预览服务已在 `http://localhost:5174/` 启动并返回页面。
- 后端服务已在 `http://localhost:8083/driving-exam` 启动。
- 已执行 `mvn.cmd -q -DskipTests install`，确保 `driving-exam-web` 单模块运行时加载最新 service/dao/entity 快照。
- 已补齐当前数据库旧表结构缺失字段：
  - `exam_record.total`
  - `wrong_question.wrong_answer`
  - `wrong_question.correct_answer`
- 补充题库数据：
  - 新增 `database/seed_more_questions_2026_05_09.sql`
  - 当前数据库题量从 33 道补充到 103 道
  - 各章节题量约 14 到 15 道
- 优化页面体验：
  - 在登录/注册/首页以外的业务页面增加全局“上一页 / 首页”浮动导航
  - 参考常见后台/工具类应用导航习惯，将全局导航从右下角调整为左上角紧凑导航窗格
  - 导航窗格显示当前页面名，并使用图标按钮提供返回上一页和返回首页
  - 题库列表由 `id DESC` 改为 `id ASC`，避免页面显示为 98 到 66 的倒序
  - 题型筛选值统一为数据库实际值：`single`、`multiple`、`judge`
- 冒烟测试通过：
  - `GET /api/category/list`
  - `POST /api/user/login`
  - `GET /api/question/list`
  - `GET /api/exam/random?limit=3`
  - `GET /api/wrong/list?userId=1`
  - `GET /api/exam/all`
  - `GET /api/config/list`
  - `GET /api/question/count` 返回 `103`

## 备注
- Tomcat 7 在 JDK 17 下会输出新版 Jar `module-info.class` 扫描警告，当前不影响 Spring 初始化和接口访问。
- PowerShell 默认输出编码会让中文 `Get-Content` 显示乱码，使用 Node 读取确认源码和文档本身为正常 UTF-8 内容。
